using Autofac;

namespace Xpressive.Home.Plugins.Email
{
    public class EmailModule : Module
    {
        protected override void Load(ContainerBuilder builder)
        {
            base.Load(builder);
        }
    }
}
